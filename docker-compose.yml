services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: geo-portfolio:latest
    ports:
      - "${APP_PORT:-3001}:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      RESEND_API_KEY: ${RESEND_API_KEY:-}
